import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.teamsmeetingmanager.app',
  appName: 'TeamsManager',
  webDir: 'dist'
};

export default config;
